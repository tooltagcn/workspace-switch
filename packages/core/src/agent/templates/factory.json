{
  "id": "factory",
  "name": "Factory",
  "configDirName": ".factory",
  "mcpFile": "config.json",
  "mcpField": "mcpServers",
  "skillDir": "skills",
  "icon": "factory",
  "targetFormat": "json-map",
  "entryFormat": {
    "format": "json-map",
    "envTransform": "${env:VAR}",
    "fieldMapping": {
      "command": "command",
      "args": "args",
      "url": "url",
      "env": "env"
    }
  }
}
